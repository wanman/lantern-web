doctype html
html(lang="en")
    head
        title=title
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet" type="text/css" href="/main.css" media="screen")

    body(id=page_id+"-view")
        block content
        script(src="/vendor/pouchdb.min.js")
        script(src="/main.js")
        block scripts